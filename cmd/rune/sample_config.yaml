# Rune user configuration
#
# Everything is commented out below; uncomment what you want to change. See
# `cmd/rune/rune.star` in the Rune source tree for the exhaustive list of
# available keys.

# Path to the debug log. This property is not reloaded on reloadWorkspace.
#log_path: ~/.rune/debug.log

#gui:
#  # Pick a theme by name. See the `guitheme` command for the full list of commands.
#  default_theme: romero
#  #font_size: 13
#  # Lines scrolled per unit of mouse-wheel movement (higher is faster).
#  #scroll_multiplier: 3
#  # Alt key that types layout characters instead of shortcuts: left or right.
#  #alt_modifier: right
#  window_opacity: { fg: 1, bg: 1 }

#editor:
#  # 'modal' (vi-like), 'standard' (conventional), 'emacs', or 'exo'.
#  mode: modal
#  autoindent: true
#  #standard:
#  #  search:
#  #    focus_frame_attr: { fg: silver, bg: default }
#  #    button_attr: { fg: default, bg: gray }
#  #    button_hover_attr: { fg: default, bg: blue }
#  # Override syntax highlight colors. Any key omitted inherits the default.
#  #highlights:
#  #  keyword: { fg: yellow }
#  #  string:  { fg: magenta }
#  status_bar:
#    enabled: true
#  aux_bar:
#    enabled: true
#    folds: true
#    git: inline           # bar | inline | all | disabled
#    lines: absolute       # disabled | absolute | relative
#    highlight_cursor: true

#command:
#  # Key that opens the command prompt.
#  key: ":"
#  # Command history length.
#  max_history: 20000
#  # Aliases layer on top of the built-ins.
#  aliases:
#    w: write
#    won:
#      command: workspaceopen
#      completer:
#        - {history}
#        - {file}
#    gitblame: "! git blame %"
#  # Key bindings merge with the built-ins; set a value to "" to unbind.
#  key_bindings:
#    <m-q>: quit
#    <m-t>: tabnew

#terminal:
#  # Default shell. Falls back to $SHELL when unset.
#  #shell: bash
#  # Maximum emulator lines.
#  max_lines: 1000
#  # Enable terminal modal mode on <esc>.
#  modal: true

#workspace:
#  auto_restore: true
#  # Override the default ASCII wallpaper.
#  #wallpaper: myAsciiArt
#  #wallpaper_image: /path/to/image.png

#telemetry:
#  # Report anonymous usage and system information. Set to false to opt out.
#  enabled: true

#extensions:
#  fuzzy_search:
#    path: extension_fuzzy_search
#    config:
#      file:
#        case_sensitive: true
#        algo: fuzzy        # fuzzy | contains | equal
#        history_key: <m-p>
